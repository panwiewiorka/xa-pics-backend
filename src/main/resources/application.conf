ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.xapics.ApplicationKt.module ]
    }
}
jwt {
    issuer = "http://0.0.0.0:8080"
;     domain = "http://0.0.0.0:8080"
    domain = "http://192.168.0.87:8080"
    domain = ${?DOMAIN}
    baseurl = "http://192.168.0.87:8080"
    baseurl = ${?BASEURL}
    audience = "users"
    realm = "XA pics"
    secret = "BFG2001"
    secret = ${?JWT_SECRET}
}